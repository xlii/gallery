<!DOCTYPE html>
<html>
<head>
  <title>Gallery2</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
	<div id="user-navigation">
    <ul>
      <li><%= current_user.name %> [<%= current_user.email%>]</li>
      <li><a href="/logout" class="logout">Sair</a></li>
    </ul>
  </div>

  <div id="menu">
    <ul>
      <li><a href="<%= galleries_path %>">Galerias</a></li>
      <li><a href="<%= new_unregistered_photo_path %>">Fotos não classificadas</a></li>      
    </ul>
  </div>

	<p class="notice"><%= notice %></p>
	<p class="alert"><%= alert %></p>
	<%= yield %>

</body>
</html>
